# Dependencies for running inference (CNN + XGBoost + stacker ensemble)
# and the Telegram bot. CPU-only torch — see Dockerfile for the install command.
numpy>=1.24
pandas>=2.0
scipy>=1.10
librosa>=0.10
soundfile>=0.12
torch>=2.1
xgboost>=2.0
scikit-learn>=1.3

# Telegram bot
python-telegram-bot>=20.0
aiohttp>=3.9
matplotlib>=3.7
pydub>=0.25v